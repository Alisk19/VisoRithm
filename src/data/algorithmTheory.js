// src/data/algorithmTheory.js
// Theory content for every algorithm slug

export const ALGORITHM_THEORY = {
  'bubble-sort': {
    definition: 'Bubble Sort is a simple comparison-based algorithm that repeatedly steps through the array, compares adjacent elements, and swaps them if they are in the wrong order. The pass is repeated until no swaps are needed.',
    steps: [
      'Start from the first element.',
      'Compare adjacent elements — swap if left > right.',
      'After each full pass, the largest unsorted element "bubbles" to its final position.',
      'Repeat for n–1 passes total.',
      'Early exit if no swaps occur in a pass (already sorted).',
    ],
    timeComplexity: { best: 'O(n)', average: 'O(n²)', worst: 'O(n²)' },
    spaceComplexity: 'O(1)',
    advantages: ['Simple to understand and implement', 'Detects already-sorted array in O(n)', 'In-place (no extra memory)'],
    disadvantages: ['Very slow for large datasets', 'O(n²) comparisons even for nearly-sorted arrays (without optimization)'],
    useCases: ['Teaching purposes', 'Sorting very small arrays', 'Detecting if a list is sorted'],
  },
  'selection-sort': {
    definition: 'Selection Sort divides the array into a sorted and unsorted region. It repeatedly finds the minimum element in the unsorted region and places it at the beginning of that region.',
    steps: [
      'Initialize a pointer i at the start.',
      'Find the minimum element in arr[i..n-1].',
      'Swap it with arr[i].',
      'Move i forward by one.',
      'Repeat until i = n–1.',
    ],
    timeComplexity: { best: 'O(n²)', average: 'O(n²)', worst: 'O(n²)' },
    spaceComplexity: 'O(1)',
    advantages: ['Simple implementation', 'Minimum swaps (O(n)) — useful when writes are expensive', 'In-place'],
    disadvantages: ['Always O(n²) — no early exit', 'Not stable (may change relative order of equal elements)'],
    useCases: ['Memory-constrained environments', 'When swap cost >> comparison cost'],
  },
  'insertion-sort': {
    definition: 'Insertion Sort builds a sorted array one element at a time by picking each new element and inserting it into its correct position among the already-sorted elements.',
    steps: [
      'Treat the first element as sorted.',
      'Pick the next element (key).',
      'Shift all sorted elements greater than key one position right.',
      'Insert key into its correct position.',
      'Repeat for all remaining elements.',
    ],
    timeComplexity: { best: 'O(n)', average: 'O(n²)', worst: 'O(n²)' },
    spaceComplexity: 'O(1)',
    advantages: ['Adaptive — O(n) for nearly-sorted data', 'Online — can sort as data arrives', 'Stable sort', 'In-place'],
    disadvantages: ['Slow for large reverse-sorted arrays', 'More writes per comparison than selection sort'],
    useCases: ['Small arrays (< 20 elements)', 'Nearly-sorted data', 'Online sorting (streaming input)'],
  },
  'merge-sort': {
    definition: 'Merge Sort is a divide-and-conquer algorithm that recursively splits the array in half, sorts each half, and merges them back together into a single sorted array.',
    steps: [
      'Divide the array into two equal halves.',
      'Recursively sort the left half.',
      'Recursively sort the right half.',
      'Merge the two sorted halves by comparing elements one by one.',
      'Base case: single element is trivially sorted.',
    ],
    timeComplexity: { best: 'O(n log n)', average: 'O(n log n)', worst: 'O(n log n)' },
    spaceComplexity: 'O(n)',
    advantages: ['Guaranteed O(n log n)', 'Stable sort', 'Well-suited for linked lists', 'Predictable performance'],
    disadvantages: ['O(n) extra space required', 'Slower than quicksort in practice due to memory allocation'],
    useCases: ['Large datasets', 'External sorting (disk-based)', 'Linked list sorting'],
  },
  'quick-sort': {
    definition: 'Quick Sort picks a pivot element and partitions the array into two sub-arrays: elements less than the pivot and elements greater. It then recursively sorts each sub-array.',
    steps: [
      'Choose a pivot (last element in Lomuto scheme).',
      'Partition: move all elements ≤ pivot to left, > pivot to right.',
      'Place pivot in its final sorted position.',
      'Recursively sort the left sub-array.',
      'Recursively sort the right sub-array.',
    ],
    timeComplexity: { best: 'O(n log n)', average: 'O(n log n)', worst: 'O(n²) — sorted input with bad pivot' },
    spaceComplexity: 'O(log n) stack space',
    advantages: ['Fastest in practice for random data', 'In-place (no extra array)', 'Cache-friendly access pattern'],
    disadvantages: ['Worst case O(n²) without randomized pivot', 'Not stable', 'Difficult to implement correctly'],
    useCases: ['General-purpose sorting', 'Systems where average performance matters', 'When in-place sort is required'],
  },
  'linear-search': {
    definition: 'Linear Search sequentially checks each element of the array until the target value is found or the end is reached. No prior sorting is required.',
    steps: [
      'Start at index 0.',
      'Compare current element with target.',
      'If equal — return current index (found!).',
      'Otherwise move to next index.',
      'If end reached without finding — return –1 (not found).',
    ],
    timeComplexity: { best: 'O(1) — target at index 0', average: 'O(n)', worst: 'O(n)' },
    spaceComplexity: 'O(1)',
    advantages: ['Works on unsorted arrays', 'Simple implementation', 'No preprocessing needed'],
    disadvantages: ['Very slow for large arrays', 'Checks every element in worst case'],
    useCases: ['Small unsorted arrays', 'Linked lists', 'One-time searches where sorting overhead is not justified'],
  },
  'binary-search': {
    definition: 'Binary Search works on a sorted array. It repeatedly halves the search space by comparing the middle element with the target, eliminating half the remaining candidates each step.',
    steps: [
      'Set left = 0, right = n–1.',
      'Compute mid = ⌊(left + right) / 2⌋.',
      'If arr[mid] == target — found!',
      'If arr[mid] < target — search right half (left = mid + 1).',
      'If arr[mid] > target — search left half (right = mid – 1).',
      'If left > right — target not found.',
    ],
    timeComplexity: { best: 'O(1)', average: 'O(log n)', worst: 'O(log n)' },
    spaceComplexity: 'O(1)',
    advantages: ['Extremely fast — eliminates half each step', 'Simple', 'Widely applicable'],
    disadvantages: ['Requires sorted array', 'Not suitable for linked lists', 'Frequent insertions break sortedness'],
    useCases: ['Searching sorted arrays', 'Dictionary lookups', 'Finding boundary conditions'],
  },
  'n-queens': {
    definition: 'The N-Queens problem places N chess queens on an N×N board so no two queens threaten each other — no two share a row, column, or diagonal. It is solved via backtracking.',
    steps: [
      'Start at row 0.',
      'Try placing a queen in each column of the current row.',
      'Check if placement is safe (no conflicts on column or diagonals).',
      'If safe — place queen and recurse to next row.',
      'If recursion fails — remove queen and try next column (backtrack).',
      'If all rows filled — solution found!',
    ],
    timeComplexity: { best: 'O(N!)', average: 'O(N!)', worst: 'O(N!)' },
    spaceComplexity: 'O(N) for recursion stack',
    advantages: ['Demonstrates backtracking elegantly', 'Can find all solutions not just one'],
    disadvantages: ['Exponential complexity', 'Infeasible for very large N'],
    useCases: ['Constraint satisfaction problems', 'Scheduling (no-conflict assignments)', 'Puzzle solving'],
  },
  'rat-in-maze': {
    definition: 'Rat in a Maze finds a path for a rat from the top-left to the bottom-right corner of a grid maze, where some cells are blocked. Uses backtracking to explore all directions.',
    steps: [
      'Start at (0,0).',
      'Mark current cell as part of solution.',
      'Try moving Down, Right, Up, Left.',
      'If a move is valid (within bounds and not blocked) — recurse.',
      'If recursion fails — backtrack (unmark cell) and try next direction.',
      'If (n–1, n–1) reached — path found!',
    ],
    timeComplexity: { best: 'O(1)', average: 'O(2^(n²))', worst: 'O(4^(n²))' },
    spaceComplexity: 'O(n²)',
    advantages: ['Finds solution if one exists', 'Can find multiple paths'],
    disadvantages: ['Exponential in worst case', 'May explore dead ends extensively'],
    useCases: ['Robot path planning', 'Maze solving games', 'Network routing'],
  },
  'subset-sum': {
    definition: 'Subset Sum determines whether any subset of a given set of integers sums to a target value. Uses backtracking (include/exclude decision tree).',
    steps: [
      'For each element decide: include or exclude.',
      'If included — add to running sum, recurse.',
      'If sum equals target — solution found!',
      'If sum exceeds target or no elements left — backtrack.',
      'Try excluding current element, recurse.',
    ],
    timeComplexity: { best: 'O(1)', average: 'O(2^n)', worst: 'O(2^n)' },
    spaceComplexity: 'O(n) recursion depth',
    advantages: ['Finds exact subset', 'Can enumerate all solutions'],
    disadvantages: ['Exponential time', 'Can be improved with DP for special cases'],
    useCases: ['Cryptography', 'Resource allocation', 'Partition problems'],
  },
  'permutations': {
    definition: 'Generates all possible arrangements of elements in an array. Uses backtracking with swapping: fix one position, recursively permute the rest, then restore (backtrack).',
    steps: [
      'Fix element at position start.',
      'Swap arr[start] with arr[i] for each i from start to n–1.',
      'Recursively permute arr[start+1..n–1].',
      'Swap back to restore original (backtrack).',
      'Base case: start == n–1 → record permutation.',
    ],
    timeComplexity: { best: 'O(n!)', average: 'O(n!)', worst: 'O(n!)' },
    spaceComplexity: 'O(n) recursion depth',
    advantages: ['Generates all possible orderings', 'In-place with swap/unswap'],
    disadvantages: ['Exponential output — n! grows very fast'],
    useCases: ['Brute force combinatorial search', 'Anagram generation', 'Scheduling permutations'],
  },
  'n-queens-optimized': {
    definition: 'An optimized N-Queens solver using bitmask operations to track column and diagonal conflicts in O(1), greatly reducing the constant factor of the backtracking search.',
    steps: [
      'Track conflicts using three bitmasks: cols, diag1, diag2.',
      'At each row find safe columns via bitwise AND/NOT.',
      'Isolate rightmost safe column with bit trick.',
      'Place queen, update masks, recurse.',
      'Backtrack by restoring previous masks.',
    ],
    timeComplexity: { best: 'O(N!)', average: 'O(N!)', worst: 'O(N!)' },
    spaceComplexity: 'O(N)',
    advantages: ['Much faster constant factor than naive backtracking', 'O(1) conflict check via bitmasks'],
    disadvantages: ['More complex to understand', 'Limited to N ≤ 64 on 64-bit systems'],
    useCases: ['Competitive programming', 'Constraint propagation benchmarking'],
  },
  '0-1-knapsack-bb': {
    definition: '0/1 Knapsack via Branch and Bound explores the decision tree of include/exclude for each item, pruning branches where the upper bound on value cannot beat the current best solution.',
    steps: [
      'Sort items by value/weight ratio (decreasing).',
      'Compute upper bound at each node using fractional relaxation.',
      'If bound ≤ current best — prune this branch.',
      'Otherwise explore include and exclude children.',
      'Update best solution when leaf reached.',
    ],
    timeComplexity: { best: 'O(n log n)', average: 'O(2^n)', worst: 'O(2^n)' },
    spaceComplexity: 'O(n)',
    advantages: ['Pruning can dramatically reduce search space', 'Finds optimal for 0/1 constraint'],
    disadvantages: ['Still exponential in worst case', 'Bound computation adds overhead'],
    useCases: ['Capital budgeting', 'Portfolio selection', 'Resource allocation'],
  },
  'job-assignment': {
    definition: 'The Job Assignment Problem assigns n workers to n jobs to minimize total cost. The Branch and Bound approach explores assignment trees, pruning using a lower-bound cost matrix.',
    steps: [
      'Model as n×n cost matrix.',
      'Assign workers to jobs one by one.',
      'At each node compute lower bound via row/column reduction.',
      'Prune if lower bound ≥ current best.',
      'Leaf node = complete assignment → update best if lower cost.',
    ],
    timeComplexity: { best: 'O(n²)', average: 'O(n!)', worst: 'O(n!)' },
    spaceComplexity: 'O(n²)',
    advantages: ['Finds globally optimal assignment', 'Pruning avoids full enumeration in practice'],
    disadvantages: ['Exponential worst case', 'Memory-intensive for large n'],
    useCases: ['Workforce scheduling', 'Machine task allocation', 'Vehicle routing'],
  },
  'bfs': {
    definition: 'Breadth First Search explores a graph level by level. Starting from a source node, it visits all neighbors before moving to neighbors of neighbors — guaranteeing shortest path in unweighted graphs.',
    steps: [
      'Enqueue the start node; mark it visited.',
      'Dequeue a node u.',
      'Visit all unvisited neighbors of u — enqueue each.',
      'Repeat until queue is empty.',
    ],
    timeComplexity: { best: 'O(V + E)', average: 'O(V + E)', worst: 'O(V + E)' },
    spaceComplexity: 'O(V)',
    advantages: ['Finds shortest path in unweighted graphs', 'Complete (finds solution if it exists)', 'Level-order traversal'],
    disadvantages: ['Uses O(V) memory for queue', 'Slower than DFS when solution is deep'],
    useCases: ['Shortest path in unweighted graphs', 'Web crawling', 'Social network level analysis', 'GPS navigation'],
  },
  'dfs': {
    definition: 'Depth First Search explores as far as possible along each branch before backtracking. Uses a stack (or recursion) to track the path.',
    steps: [
      'Push start node onto stack.',
      'Pop node u; if unvisited — mark visited.',
      'Push all unvisited neighbors of u.',
      'Repeat until stack is empty.',
    ],
    timeComplexity: { best: 'O(V + E)', average: 'O(V + E)', worst: 'O(V + E)' },
    spaceComplexity: 'O(V)',
    advantages: ['Low memory for sparse graphs', 'Useful for cycle detection and topological sort', 'Simple recursive implementation'],
    disadvantages: ['Does not guarantee shortest path', 'Can get stuck in infinite loops without visited tracking'],
    useCases: ['Maze solving', 'Topological sorting', 'Cycle detection', 'Finding connected components'],
  },
  'prims': {
    definition: "Prim's Algorithm builds a Minimum Spanning Tree (MST) greedily. Starting from any node, it always adds the cheapest edge that connects a new node to the growing MST.",
    steps: [
      'Initialize key[source]=0, all others=∞.',
      'Pick the unvisited node u with minimum key.',
      'Add u to MST.',
      'For each unvisited neighbor v of u: if edge(u,v) < key[v] → update key[v] and parent[v].',
      'Repeat until all nodes are in MST.',
    ],
    timeComplexity: { best: 'O(E log V)', average: 'O(E log V)', worst: 'O(V²) — adjacency matrix' },
    spaceComplexity: 'O(V)',
    advantages: ['Efficient with priority queue', 'Good for dense graphs', 'Always produces MST'],
    disadvantages: ['Harder to implement with priority queue', 'Requires all weights to be numeric'],
    useCases: ['Network design', 'Circuit design', 'Approximation for TSP'],
  },
  'kruskal': {
    definition: "Kruskal's Algorithm builds a Minimum Spanning Tree by sorting all edges by weight and greedily adding the cheapest edge that doesn't form a cycle, using Union-Find to detect cycles.",
    steps: [
      'Sort all edges by weight.',
      'Initialize each node as its own component (Union-Find).',
      'For each edge (u, v, w) in sorted order:',
      '  If find(u) ≠ find(v) — no cycle → add edge to MST, union(u, v).',
      'Stop when n–1 edges added.',
    ],
    timeComplexity: { best: 'O(E log E)', average: 'O(E log E)', worst: 'O(E log E)' },
    spaceComplexity: 'O(V + E)',
    advantages: ['Simple to implement', 'Very efficient for sparse graphs', 'Easy to parallelize'],
    disadvantages: ['Requires sorting all edges upfront', 'Union-Find adds implementation complexity'],
    useCases: ['Sparse graph MST', 'Network design', 'Image segmentation'],
  },
  '0-1-knapsack-dp': {
    definition: 'The 0/1 Knapsack using Dynamic Programming fills an (n+1)×(W+1) table where dp[i][w] = maximum value using the first i items with weight capacity w.',
    steps: [
      'Initialize dp[0][*] = dp[*][0] = 0.',
      'For each item i and each capacity w:',
      '  If weight[i] ≤ w: dp[i][w] = max(val[i] + dp[i–1][w–wt[i]], dp[i–1][w]).',
      '  Else: dp[i][w] = dp[i–1][w] (cannot include).',
      'Answer is dp[n][W].',
    ],
    timeComplexity: { best: 'O(nW)', average: 'O(nW)', worst: 'O(nW)' },
    spaceComplexity: 'O(nW)',
    advantages: ['Polynomial for fixed W', 'Finds exact optimum', 'Easy to implement'],
    disadvantages: ['Pseudo-polynomial — O(nW) depends on W value', 'Large W → huge table'],
    useCases: ['Cargo loading', 'Portfolio optimization', 'Resource allocation'],
  },
  'fibonacci-dp': {
    definition: 'Fibonacci DP computes Fibonacci numbers bottom-up, storing each result in a table to avoid redundant recursion. dp[i] = dp[i-1] + dp[i-2].',
    steps: [
      'Set dp[0] = 0, dp[1] = 1.',
      'For i from 2 to n: dp[i] = dp[i-1] + dp[i-2].',
      'Return dp[n].',
    ],
    timeComplexity: { best: 'O(n)', average: 'O(n)', worst: 'O(n)' },
    spaceComplexity: 'O(n) — can be O(1) with rolling variables',
    advantages: ['Eliminates exponential recursion', 'Clear DP table visualization'],
    disadvantages: ['Overkill for small n', 'Grows extremely fast — needs BigInt for large n'],
    useCases: ['Teaching DP concepts', 'Sequence generation', 'Golden ratio approximation'],
  },
  'lcs': {
    definition: 'Longest Common Subsequence finds the longest sequence of characters present in both strings in order, but not necessarily contiguous. Uses a 2D DP table.',
    steps: [
      'Build (m+1)×(n+1) table, base case = 0.',
      'If s1[i-1] == s2[j-1]: dp[i][j] = dp[i-1][j-1] + 1.',
      'Else: dp[i][j] = max(dp[i-1][j], dp[i][j-1]).',
      'LCS length = dp[m][n].',
      'Backtrack through table to recover actual sequence.',
    ],
    timeComplexity: { best: 'O(mn)', average: 'O(mn)', worst: 'O(mn)' },
    spaceComplexity: 'O(mn)',
    advantages: ['Exact optimal solution', 'Works for any alphabet'],
    disadvantages: ['O(mn) space can be large', 'Only length by default — backtracking needed for sequence'],
    useCases: ['DNA sequence alignment', 'Diff tools (git diff)', 'Spelling correction'],
  },
  'activity-selection': {
    definition: 'Activity Selection greedily selects the maximum number of non-overlapping activities from a set of activities each with start and finish times.',
    steps: [
      'Sort activities by finish time.',
      'Always select the first activity.',
      'For each subsequent activity: if its start time ≥ last selected finish time → select it.',
      'Otherwise skip it.',
    ],
    timeComplexity: { best: 'O(n log n)', average: 'O(n log n)', worst: 'O(n log n)' },
    spaceComplexity: 'O(1)',
    advantages: ['Greedy gives optimal solution', 'Simple and efficient', 'No DP table needed'],
    disadvantages: ['Only works for maximizing count — not value', 'Requires sorting'],
    useCases: ['Meeting room scheduling', 'CPU job scheduling', 'Classroom allocation'],
  },
  'fractional-knapsack': {
    definition: 'Fractional Knapsack allows taking fractions of items. The greedy approach of always taking the item with highest value/weight ratio is provably optimal.',
    steps: [
      'Compute value/weight ratio for each item.',
      'Sort items by ratio descending.',
      'Take each item fully if capacity allows.',
      'Take fraction of the next item when capacity runs out.',
      'Stop when capacity is 0.',
    ],
    timeComplexity: { best: 'O(n log n)', average: 'O(n log n)', worst: 'O(n log n)' },
    spaceComplexity: 'O(1)',
    advantages: ['Greedy is optimal for fractional case', 'Simple implementation'],
    disadvantages: ['Does NOT give optimal for 0/1 knapsack (greedy fails there)'],
    useCases: ['Resource allocation with divisible resources', 'Investment portfolio with partial positions'],
  },
  'warshalls': {
    definition: "Warshall's Algorithm computes the transitive closure of a directed graph — reach[i][j]=1 if there exists any path from vertex i to vertex j.",
    steps: [
      'Start with the adjacency matrix as the reach matrix.',
      'For each intermediate vertex k:',
      '  For each pair (i, j): reach[i][j] = reach[i][j] OR (reach[i][k] AND reach[k][j]).',
      'After all k iterations, reach[i][j]=1 iff i can reach j.',
    ],
    timeComplexity: { best: 'O(V³)', average: 'O(V³)', worst: 'O(V³)' },
    spaceComplexity: 'O(V²)',
    advantages: ['Simple O(V³) iteration', 'Computes full reachability in one pass'],
    disadvantages: ['O(V³) — slow for large graphs', 'Only gives Boolean reachability, not path lengths'],
    useCases: ['Reachability queries', 'Compiler optimizations (data flow)', 'Database query optimization'],
  },
  'floyds': {
    definition: "Floyd-Warshall computes shortest paths between ALL pairs of vertices. It iteratively relaxes all paths through each intermediate vertex k.",
    steps: [
      'Initialize dist[u][v] = edge weight if edge exists, else ∞; dist[v][v]=0.',
      'For each intermediate k:',
      '  For each pair (i, j): dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j]).',
      'After V iterations, dist[i][j] = shortest path i→j.',
    ],
    timeComplexity: { best: 'O(V³)', average: 'O(V³)', worst: 'O(V³)' },
    spaceComplexity: 'O(V²)',
    advantages: ['All-pairs shortest paths in one pass', 'Handles negative edges (not cycles)', 'Simple to implement'],
    disadvantages: ['O(V³) — infeasible for very large graphs', 'Cannot handle negative cycles'],
    useCases: ['Network routing tables', 'All-pairs distance queries', 'Detecting negative cycles'],
  },
  'dc-merge-sort': {
    definition: 'Merge Sort (Divide & Conquer) — see Merge Sort under Sorting.',
    steps: ['Same as Merge Sort.'],
    timeComplexity: { best: 'O(n log n)', average: 'O(n log n)', worst: 'O(n log n)' },
    spaceComplexity: 'O(n)',
    advantages: ['Guaranteed O(n log n)', 'Stable'],
    disadvantages: ['O(n) extra space'],
    useCases: ['Large dataset sorting'],
  },
  'dc-quick-sort': {
    definition: 'Quick Sort (Divide & Conquer) — see Quick Sort under Sorting.',
    steps: ['Same as Quick Sort.'],
    timeComplexity: { best: 'O(n log n)', average: 'O(n log n)', worst: 'O(n²)' },
    spaceComplexity: 'O(log n)',
    advantages: ['In-place', 'Cache-friendly'],
    disadvantages: ['Worst-case O(n²)'],
    useCases: ['General sorting'],
  },
  'dc-binary-search': {
    definition: 'Binary Search (Divide & Conquer) — see Binary Search under Searching.',
    steps: ['Same as Binary Search.'],
    timeComplexity: { best: 'O(1)', average: 'O(log n)', worst: 'O(log n)' },
    spaceComplexity: 'O(1)',
    advantages: ['O(log n)'],
    disadvantages: ['Requires sorted input'],
    useCases: ['Sorted array lookups'],
  },
};

export function getAlgorithmTheory(slug) {
  return ALGORITHM_THEORY[slug] || null;
}
